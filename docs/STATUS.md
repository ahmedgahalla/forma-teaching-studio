# Project status

Auditor-owned (Claude Code); updated after each merge. Builder PRs do not edit this file. Last updated: 2026-09-26, after the PR #6 merge.

## Done

- **PR #6 — PR lifecycle delegation, merged to main** as `804f28c` (merge commit), branch deleted. Naser delegated the full PR lifecycle (create/update/comment/audit/merge) to Claude Code via `gh`, now a standing rule in CLAUDE.md. Also recorded lesson 9a in `docs/lessons-learned.md`: the repo's `protect-main` ruleset requires a status check literally named `frontend`, which no longer exists once a job runs as a matrix (see Pending below). Doc: `docs/phases/phase-1-tooling/1.6-pr-lifecycle-delegation.md`.
- **Phase 2 — refactor: merged to main** as `a6dc7c9` (PR #2, merge commit), tagged **`v0.14-phase2`**, branch deleted. Dead code removed with per-candidate verification (incl. the legacy `/api/interpret` route and 119 dead CSS rules); Studio.tsx 5,123 → 1,011 lines (16 state hooks, typed `CaseStudioApi`, ~20 views, handler factories); all stylesheets as order-preserving partials; classroom.ts and backend main.py split behind stable seams; components in eight feature folders; zero TODO lint disables (17 standing, individually justified); size allowlist at 24 permanent ADR-gated entries. Full record: `docs/phases/phase-2-refactor/`, ADR 003, `docs/architecture/folder-structure.md`. Gate at merge: frontend 1,575/1,575 · backend 470/470 · CI green on GitHub · six screens pixel-identical to the committed baselines (DPR 1+2).
- **Phase 1 — tooling: merged** as `cac430b`, tagged `v0.13-phase1` (rulebook, Prettier/ESLint, LF, CI, docs system, setup automation). Record: `docs/phases/phase-1-tooling/`.
- v0.12 feature set (pre-phase main): case workspace, workflow classrooms, Try Mode, mechanics experiments, AI command interpretation, lecture mode, the 28-tooth Blender asset. See `docs/VERIFICATION.md`.

## In progress

- **PR #5 — Node version policy** (`naser/node-version-policy`): CI green on `backend`, `frontend (22)`, `frontend (24)`; zero blocking findings; phase doc (1.5) in place. **Blocked from merging** by the branch-protection issue below, not by CI or review — will merge as soon as it's cleared. Also awaiting the builder's sign-off on its AGENTS.md change (`docs/reviews/from-auditor/2026-09-26-node-version-policy-agents-change.md`, non-blocking per Naser).
- Open auditor follow-ups are filed in `docs/reviews/from-auditor/` (narrow case-view props; split try-mode.ts; CSS dedupe; the PR #5 AGENTS.md sign-off above).

## Pending (external)

- **Branch protection is enabled** (`protect-main` ruleset, confirmed via the API on 2026-09-26) — the earlier ask for the owner to set this up is done. But its required status checks are named `frontend` and `backend`, and `frontend` no longer exists as a reported check once a job runs as a matrix (PR #5 splits it into `frontend (22)` / `frontend (24)`), so any PR touching that job — including #5 itself — can never satisfy the rule and is permanently unmergeable through normal means. **Needs the repo owner (admin access; `NaserShadid` has write only, confirmed `permissions.admin: false`) to update the ruleset's required checks to `backend`, `frontend (22)`, `frontend (24)`.** See lesson 9a.

## Next

- **Phase 3 — demo path**: awaiting Naser's spec before any work starts. Scope sketch in `docs/phases/phase-3-demo-path/README.md` (large full-bite opening view, advanced-tool drawer, scripted 3–4 minute walkthrough).
- Standing demo context: the audience is an orthodontist who lectures at universities; presentable beats feature-complete.
