# Project status

Auditor-owned (Claude Code); updated after each merge. Builder PRs do not edit this file. Last updated: 2026-09-26, after the PR #3 merge.

## Done

- **PR #3 — shared project workflow, merged to main** as `7127a29` (merge commit), branch deleted. Builder-authored docs: dedicated local checkout convention, pull/branch/PR workflow, environment notes. Doc: `docs/phases/phase-1-tooling/1.5-shared-project-workflow.md`. Audit: `docs/audits/2026-09-26-ahmed-local-project-setup.md` (zero Blocking findings; one non-blocking suggestion).
- **PR #5 — Node version policy, merged to main** as `538cfd7` (merge commit), branch deleted. Node 22+ supported (floor 22.6), CI matrix on Node 22 and 24, policy documented in AGENTS.md and README. Its phase doc was renumbered 1.5 → **1.7** before merge (1.5 was reserved for PR #3, 1.6 was already taken) — see lesson 10. Required a repo-owner fix first: the `protect-main` ruleset's required status checks didn't match the actual CI job names (see lesson 9a); owner corrected it mid-session to `backend`, `frontend (22)`, `frontend (24)`, confirmed via the API before merging.
- **PR #6 — PR lifecycle delegation, merged to main** as `804f28c` (merge commit), branch deleted. Naser delegated the full PR lifecycle (create/update/comment/audit/merge) to Claude Code via `gh`, now a standing rule in CLAUDE.md. Doc: `docs/phases/phase-1-tooling/1.6-pr-lifecycle-delegation.md`.
- **Phase 2 — refactor: merged to main** as `a6dc7c9` (PR #2, merge commit), tagged **`v0.14-phase2`**, branch deleted. Dead code removed with per-candidate verification (incl. the legacy `/api/interpret` route and 119 dead CSS rules); Studio.tsx 5,123 → 1,011 lines (16 state hooks, typed `CaseStudioApi`, ~20 views, handler factories); all stylesheets as order-preserving partials; classroom.ts and backend main.py split behind stable seams; components in eight feature folders; zero TODO lint disables (17 standing, individually justified); size allowlist at 24 permanent ADR-gated entries. Full record: `docs/phases/phase-2-refactor/`, ADR 003, `docs/architecture/folder-structure.md`. Gate at merge: frontend 1,575/1,575 · backend 470/470 · CI green on GitHub · six screens pixel-identical to the committed baselines (DPR 1+2).
- **Phase 1 — tooling: merged** as `cac430b`, tagged `v0.13-phase1` (rulebook, Prettier/ESLint, LF, CI, docs system, setup automation). Record: `docs/phases/phase-1-tooling/`.
- v0.12 feature set (pre-phase main): case workspace, workflow classrooms, Try Mode, mechanics experiments, AI command interpretation, lecture mode, the 28-tooth Blender asset. See `docs/VERIFICATION.md`.

## In progress

- Nothing on a branch. Open auditor follow-ups are filed in `docs/reviews/from-auditor/` (narrow case-view props; split try-mode.ts; CSS dedupe; PR #5's AGENTS.md sign-off — builder should mark resolved or wontfix next session).

## Pending (external)

- Branch protection (`protect-main` ruleset) is enabled and its required status checks now correctly match the live CI job names (`backend`, `frontend (22)`, `frontend (24)`) — confirmed via the API on 2026-09-26. No outstanding action. If a future CI job gains/loses a matrix dimension or is renamed, the ruleset needs a matching update (repo-owner/admin action) — see lesson 9a.

## Next

- **Phase 3 — demo path**: awaiting Naser's spec before any work starts. Scope sketch in `docs/phases/phase-3-demo-path/README.md` (large full-bite opening view, advanced-tool drawer, scripted 3–4 minute walkthrough).
- Standing demo context: the audience is an orthodontist who lectures at universities; presentable beats feature-complete.
