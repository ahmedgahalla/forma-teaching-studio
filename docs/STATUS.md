# Project status

Auditor-owned (Claude Code); updated after each merge. Builder PRs do not edit this file. Last updated: 2026-09-25, after the Phase 1 merge (update rides in Phase 2's first commit per AGENTS.md § after-merge).

## Done

- **Phase 1 — tooling: merged to main** as `cac430b` (PR #1, merge commit), tagged **`v0.13-phase1`**, branch deleted. Shared rulebook in AGENTS.md with session protocols and merge rules, Prettier + repo-wide reformat, ESLint 9 at zero warnings, LF line endings, environment pinning, CI (green on GitHub for both jobs), PR template, `check:limits`, reviews inbox, lessons-learned loop (×8), numbered phase docs, baselines, `npm run setup` + sync hooks, teaching-case audit fixed. Full record: `docs/phases/phase-1-tooling/`.
- v0.12 feature set (pre-Phase-1 main `0b2e7db`): case workspace, workflow classrooms, Try Mode, mechanics experiments, AI command interpretation, lecture mode, the 28-tooth Blender asset. See `docs/VERIFICATION.md` for the running verification log.
- Cached-BVH collision optimization is applied in `src/lib/analysis.ts` (`analysis.bvh.test.ts` is its regression oracle); the stale claim in `docs/PRODUCT_DIRECTION.md` was corrected in Phase 1 (lessons-learned #5).

## In progress

- **Phase 2 — refactor** (`naser/phase-2-refactor`, on merged main): slice 2.1 dead-code removal under way (workflow-verified candidates; `useSpeech.ts`, dead speech controller, `exportSTL`, 27 unexports, dead CSS, legacy `/api/interpret`). Then: Studio.tsx split, CSS split, other oversized files, backend routers, feature folders, resolving the 32 `TODO(phase-2)` lint disables, shrinking the `check:limits` allowlist. See `docs/phases/phase-2-refactor/`.

## Pending (external)

- Owner to apply the GitHub repo settings: branch protection on main (require PR + green CI, block direct pushes), merge-commit-only, and auto-delete of head branches. Naser has no admin access. Until then the auditor self-enforces: never push main, never merge red CI, delete branches manually after merge. Once confirmed, verify via the API and remove this item.

## Next

- **Phase 3 — demo path** (planned, not started): open into a large full-bite view, advanced tooling in a drawer, scripted 3–4 minute walkthrough. See `docs/phases/phase-3-demo-path/`.
- Standing demo context: the demo audience is an orthodontist who lectures at universities; presentable beats feature-complete.
