# 1.3 — Test fix and docs

**What was done**

- **Stale teaching-case audit fixed.** `src/lib/teaching-case-audit.test.ts` had been failing on main: `teaching-case-audit.json`'s `caseSourceSha256` no longer matched `src/lib/teaching-cases.ts` (stale since commit `7fc4d2b`, where both files landed together but the JSON was generated from an earlier source revision). The asset and metadata hashes were fine — the failure was the case-source hash. Regenerated with `node --experimental-strip-types scripts/audit-teaching-cases.mjs` after the reformat; only the source hash changed, the geometry results are byte-identical. The suite is fully green for the first time in this repo's history on this branch.
- **Docs system created**: `docs/STATUS.md` (auditor-owned), `docs/phases/` (numbered, indexed README, this folder), `docs/decisions/` ADRs 001–002, `docs/reviews/` inbox, `docs/lessons-learned.md`, `docs/handoffs/`. Existing guides untouched — every pre-existing file in `docs/` keeps its name and location, since README.md/AGENTS.md and the guides cross-link them (only `UPGRADE_PLAN.md` has no inbound links).
- **No private state**: the auditor's machine-local memory note about this project was migrated into these docs and deleted; repo docs are now the single source of truth (ADR 002).

**Decisions:** the audit JSON stays enforced by its vitest hash test (runs in CI via `npm test`) rather than a duplicate CI step — one mechanism, already exact.

**Evidence:** frontend suite on this branch: 1,599/1,599 passing (was 1,598/1,599 with the audit failure). Backend: 531/531. Full gate results in the phase README.
