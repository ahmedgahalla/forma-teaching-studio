# Phase 1.6 — builder onboarding verification

**Branch:** `ahmed/onboarding-verification` · **Owner:** builder · **Date:** 2026-09-26

## Scope

Run the requested first-session onboarding against merged Phase 2 before starting any product work. Phase 1.5 is reserved by the existing shared-workflow PR #3; this record does not depend on that unmerged change.

## Repository and workflow

- Pulled clean `main` to `93812d9`, containing Phase 2 PR #2 and its status follow-up PR #4, then created the builder branch.
- Updated the dedicated Forma checkout. A later full-folder rename for a fresh clone was blocked because the app held the repository open; instead the backend directory was preserved outside the working tree and rebuilt from unchanged Git source to remove an inaccessible old test cache. The existing `backend/.env` was restored locally and remains ignored; its values were not displayed or committed.
- Read the complete root `AGENTS.md`, architecture overview and folder structure, status, lessons, current phase, latest handoff, and three open auditor notes.
- The root `AGENTS.md` already supplies the requested persistent project instructions. No global Codex instructions or shared rulebook changes were needed.
- Builder works on `ahmed/...`, never pushes to main, never edits `docs/STATUS.md`, updates numbered phase documentation and handoffs, and submits PRs. Claude Code audits and merges with a merge commit only after green CI and zero Blocking findings.
- Phase 3 waits for Naser's specification. The three existing auditor follow-ups remain open and unchanged: case-view props, Try Mode splitting, and CSS deduplication.

## GitHub governance

Applied after confirming that Phase 2 was merged:

- Active `protect-main` ruleset, ID `24035163`, targets the default branch with no bypass actors.
- Pull requests required, zero required approvals, `frontend` and `backend` required from GitHub Actions.
- Branch deletion and force pushes blocked.
- Merge commits enabled; squash and rebase merging disabled; automatic head-branch deletion enabled.

The auditor-owned status page must be updated through the auditor's workflow to remove the completed external-settings item.

## Environment and verification

Initial defaults were Node 24.14.0 and Python 3.10.10; Python 3.13.3 was already installed.

- Installed official Node 22.23.3 side by side under the user's local Programs directory, verifying its archive against the published SHA-256. npm is 10.9.9; Git remains 2.49.0.windows.1.
- Added an idempotent PATH-priority block to the user's all-host profiles for Windows PowerShell 5.1 and PowerShell 7. Fresh sessions in both resolve Node 22.23.3 and Python 3.13.3. Existing runtimes and the existing host profile were preserved.
- `npm run setup` completed successfully using those versions: frontend and backend dependencies installed, dependency-sync hooks and blame-ignore configuration enabled. npm reported zero vulnerabilities; package-deprecation and pip-update notices were non-fatal.
- The backend `.env` stayed ignored, and model/source audit inputs retain LF line endings.
- The first backend run passed with a cache-permission warning, and lint failed while scanning that cache. Ownership repair and moving the cache itself were denied. Preserving its readable parent directory outside the repository and recreating the backend from Git allowed setup to create a fresh Python 3.13 environment without changing tracked backend files. The final gate below supersedes that initial failure.

Verification passed: 1,575 frontend tests, 470 backend tests without warnings, typecheck, lint with zero warnings, formatting, file limits, production build, and a functional browser smoke check. Details and the initial repaired permissions failure are recorded in the [session handoff](../../handoffs/2026-09-26-ahmed-onboarding-verification.md).

No application source, model assets, dependency versions, or Phase 3 features are changed by this onboarding task.
