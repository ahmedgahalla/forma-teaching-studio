# 1.1 — Rulebook and CI

**What was done**

- `AGENTS.md` rewritten as the complete, self-executing shared rulebook: session start/end protocols, project purpose, architecture pointer, repo layout, code standards, branch governance, team workflow with roles and merge rules, documentation rules, learning loop, verification gate. Shared content sits outside the auto-generated Next.js block (which `next dev` rewrites), with a comment explaining why.
- Workflow overrides applied per Naser's direction: `docs/STATUS.md` is auditor-owned (builder PRs never touch it), and the auditor merges PRs when CI is green + zero Blocking findings + phase docs updated, with merge commits only (squash/rebase would break `.git-blame-ignore-revs`). Nothing is ever pushed directly to main.
- `CLAUDE.md` reduced to `@AGENTS.md` + the audit procedure (checkout, review checklist, gate + DPR 1/2 browser check, `docs/audits/` report, lessons feed, PR comment, mechanical fixes on `naser/audit-<branch>`, merge decision, post-merge steps).
- Architecture map moved to `docs/architecture/overview.md` (updated for `0b2e7db`, including the note that the cached-BVH optimization is applied despite PRODUCT_DIRECTION.md's stale claim).
- `.github/workflows/ci.yml`: frontend job (typecheck, lint, format:check, check:limits, tests, build) on Node 22 via `.nvmrc`, backend job (pytest) on Python 3.13. No secrets needed.
- `.github/pull_request_template.md`: what/why, phase, screenshots, docs checklist, standards checklist.
- Cross-review inbox `docs/reviews/` (README + `from-builder/` + `from-auditor/`), learning loop `docs/lessons-learned.md` seeded with six lessons, `docs/handoffs/` started.
- Environment pinning: `.nvmrc` (22), `engines: node >=22.6` (the strip-types scripts need 22.6), backend documented at Python 3.13, CI pinned to both.
- Secrets audit: pattern scan (`sk-`, `ghp_`/`gho_`, `AKIA…`, key assignments) over the working tree and every commit in history — clean. Rule recorded in AGENTS.md: keys only in local, gitignored `.env` files.

**Files touched:** `AGENTS.md`, `CLAUDE.md`, `docs/architecture/overview.md`, `.github/**`, `docs/reviews/**`, `docs/lessons-learned.md`, `.nvmrc`, `package.json` (engines, scripts).

**Decisions:** ADR 001 (formatting/linting), ADR 002 (builder/auditor workflow — merge authority, STATUS ownership, session protocols, learning loop, no private state).

**Skipped / notes**

- Branch protection, default merge method, and auto-delete of head branches are GitHub settings only Naser can set (listed in the Phase 1 report).
- The stale PRODUCT_DIRECTION.md BVH paragraph was _flagged_ (STATUS.md + lessons #5) but not edited here: this phase avoids content edits to builder-owned docs; it should be corrected in the next PR touching that doc.
