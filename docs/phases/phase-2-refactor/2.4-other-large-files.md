# 2.4 — Other large files, disables, folders

**Split** (commits `2aa997d`+fix `781edba`, `8656f7e`):

- `src/lib/classroom.ts` (1,701) → an eight-module `classroom/` package (types+context helpers, validate-action, advance, plan-validate, parse-clauses, parse-try, plan-build, context) behind a stable barrel. The split briefly landed red because a stale incremental tsbuildinfo hid missing imports — `typecheck` now runs `--incremental false` (lessons #9).
- `TeachingCommandBar` (287 lines) out of TeachingController.tsx (856 → 570), re-exported for path stability.
- `LESSONS` scripted demonstrations → `src/lib/lessons.ts` per the content-as-data rule.

**Disables ledger** (commit `dcef393`): of Phase 1's 33 directives, **16 resolved for real** — 5 hook-setter deps (2.2b), 4 obsoleted by extraction, 5 Studio memo/effect deps rebuilt on extracted primitives (`scenarioCaseId`, `scenarioVariantId`, destructured `plan.current`) with identical memo behavior, 2 ref-writes-in-render converted to the effect form. **17 remain, zero TODOs**, each with a standing site-specific reason: sync-from-committed-props edit state (MechanicsPanel ×3, TryPanel ×2, LectureConsole, StudioTheme), mount-only effects (TeachingController ×2, Viewer, WorkflowStudio, MechanicsPanel deps, TryPanel deps), test-harness doubles (×2), the phase-3 full-reload anchor, Studio's file-scoped factory-wiring note, and the scan-timer cleanup line.

**Folder organization** (commit `018b5c1`): 35 files moved into `components/{case,viewer,teaching,workflow,lecture,mechanics,try,shared}/` with a resolver-based import rewriter (see `docs/architecture/folder-structure.md`). First rewrite pass silently missed most sites because Windows' case-insensitive `existsSync` resolved `'../Viewer'` to the new `viewer/` directory — the fixed pass resolves extensions first and requires file-ness. Allowlist regenerated: 24 oversize entries (was 30), each justified; 3 long-line entries.

**Skipped, with reasons** (ADR 003 has the fuller rationale): Viewer.tsx (WebGL lifecycle cohesion — splitting the renderer effect invites disposal bugs), TryPanel/MechanicsPanel/WorkflowStudio/StudioExperience (single-feature cohesive views), try-mode.ts and the grammars/solvers (single-algorithm files), content files (authored prose). Each remains barred from growing and listed in the allowlist.
